# Upper bounds keep a new major release of a dependency from breaking the
# deploy build without warning (unbounded ranges had started resolving to
# pandas 3.x / numpy 2.x on Render).
Flask>=3.0.0,<4.0.0
requests>=2.32.0,<3.0.0
nltk==3.9.1
scikit-learn>=1.5.0,<2.0.0
numpy>=1.26.0,<3.0.0
scipy>=1.13.0,<2.0.0
# pandas removed - not imported anywhere in the app, and it was the largest
# wheel in the build (see requirements-local.txt if you want it for notebooks)
jinja2>=3.1.0,<4.0.0
python-dotenv>=1.0.0,<2.0.0
gunicorn>=22.0.0,<24.0.0
# psycopg2-binary>=2.9.0  # Removed - no database needed
psutil>=5.9.0,<8.0.0
