# Minimal requirements for local testing (no C++ dependencies)
Flask>=3.0.0
requests>=2.32.0
nltk>=3.9.0
jinja2>=3.1.0
python-dotenv>=1.0.0

# Optional: lightweight alternatives
# Use these if you can't install the full scientific stack
# pip install --no-deps scikit-learn  # Skip heavy dependencies